# Terraform Docker Container Example

This project provisions a local Docker container using Terraform.

## Steps to Run
1. terraform init  
2. terraform plan  
3. terraform apply  
4. terraform state list  
5. terraform destroy

## Expected Result
A running NGINX container accessible on http://localhost:8080
